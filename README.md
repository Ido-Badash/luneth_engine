# Luneth Engine

Luneth Engine is a lightweight Python engine for 2D games. 
It includes a state manager, time manager, shared settings, and utilities to expand any kind of game.

## Installation

Install locally:
```bash
pip install -e "path/to/luneth_engine"
